- name: package installation on hosts
  hosts: all
  become: yes
  tasks:
    - name: install packages
      ansible.builtin.packages:
        name: 
          - httpd
          - nginx
        state: present
   